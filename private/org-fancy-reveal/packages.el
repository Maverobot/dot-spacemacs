;;; packages.el --- org-fancy-reveal layer packages file -*- lexical-binding: t; -*-

;;; Commentary:
;; Private Spacemacs layer that adds reusable semantic layout helpers for
;; org-re-reveal presentations.

;;; Code:

(defconst org-fancy-reveal-packages '()
  "No external packages are installed by this layer.")

;;; packages.el ends here
